import { Button } from "Y/components/ui/button";
import { useClientStore } from "Y/store";

const Navbar = () => {
  return (
    <header>
      <nav className="absolute right-4 top-4 flex items-center gap-2">
        <h1></h1>
      </nav>
    </header>
  );
};

export default Navbar;
